<!-- 改编自：综合实用主题项目提示词/cy-315/cy-315.md；后端由 FastAPI (Python) 改造为 Go 1.22 + Gin + GORM -->
请根据以下要求生成项目代码和 README。

## 项目主要需求

**项目概述**：教室排课助手是一个纯后端服务，为学校和培训机构提供课程表编排、教室资源管理和冲突检测的 RESTful API 服务。
**项目类型**：纯后端服务

### 功能需求

1. **基础数据管理**：提供完整的 CRUD API 管理：
   - 教室：教室编号、名称、容量、设备配置（投影仪/电脑/白板等）
   - 教师：姓名、工号、联系方式、授课科目、不可排课时间段
   - 班级/学生组：班级名称、学生人数、所属年级
   - 课程：课程名称、课程代码、课时长度、需要教室类型
   - 时间段：定义可用排课时段（如每天 8:00-9:40、10:00-11:40、14:00-15:40、16:00-17:40）

2. **智能排课算法**：核心功能，根据输入条件自动生成课程表：
   - 输入：学期周数、每周天数、每天节数、课程列表（含周课时要求）、教师列表、班级列表、教室列表
   - 约束条件：同一教师不能同时上两节课、同一班级不能同时上两节课、同一教室不能同时容纳两个班级、教师不可排课时段避开、优先满足连排需求（如体育课希望连排2节）
   - 输出：完整的课程表（每节课：时间、教室、教师、班级、课程）
   - 算法要求：基于回溯或贪心算法，在合理时间内生成可行解。如果无法满足所有约束，返回冲突列表并尽可能生成部分课表。

3. **冲突检测与报告**：对已有课表或排课结果进行冲突检测：
   - 教师时间冲突（同一教师同一时段多节课）
   - 班级时间冲突（同一班级同一时段多节课）
   - 教室时间冲突（同一教室同一时段多节课）
   - 教室容量冲突（班级人数 > 教室容量）
   - 教师偏好冲突（安排在教师不可排课时段）
   冲突检测报告包含冲突类型、涉及实体、建议解决方案。

4. **课表查询与导出**：
   - 按班级查询课表（返回该班级一周完整课表）
   - 按教师查询课表（返回该教师一周授课安排）
   - 按教室查询课表（返回该教室一周使用安排）
   - 支持导出为 JSON 或 CSV 格式
   - 支持按周次查看课表（适用于不同周课程不同的情况）

5. **调课与手动调整**：支持对已生成课表进行手动调整：
   - 交换两节课的时间和地点
   - 将某节课移动到另一个空闲时段
   - 每次调整后自动重新检测冲突
   - 调课历史记录

6. **统计与利用率分析**：
   - 教室利用率统计（各教室每周使用时长/总可用时长）
   - 教师工作量统计（每位教师周课时数）
   - 课程分布热力图数据（每天各时段课程密度）

## 技术要求

- **英文短名（用于 Docker Compose）**：`gbschedule`
- **后端端口**：`19515`
- **项目类型**：纯后端服务（RESTful API，无前端页面）
- **后端框架**：Go 1.22 + Gin
- **数据库**：SQLite（轻量级，无需额外数据库服务）
- **ORM**：GORM
- **数据库驱动**：github.com/glebarez/sqlite
- **数据验证**：gin 绑定与结构体校验（binding 标签）
- **API 文档**：使用 github.com/swaggo/swag + github.com/swaggo/gin-swagger 自动生成 Swagger UI（挂载于 `/docs`，OpenAPI JSON 位于 `/swagger/doc.json`）
- **其他依赖**：github.com/gin-gonic/gin、gorm.io/gorm、github.com/glebarez/sqlite、github.com/swaggo/swag、github.com/swaggo/gin-swagger
## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。


## 部署要求

纯后端服务，提供 API 接口。数据存储使用 SQLite（文件级数据库，无需独立数据库容器）。

后端代码放在 `backend/`，采用 Go 标准布局：
```
backend/
├── go.mod
├── cmd/server/main.go
└── internal/
    ├── config/
    ├── model/
    ├── repository/
    ├── service/
    ├── handler/
    ├── router/
    ├── middleware/
    ├── dto/
    └── constants/
```

后端代码组织约束（Go 等价表述）：
- handler 只负责 HTTP 请求参数绑定、响应和错误转换，不直接写业务逻辑
- service 负责业务逻辑（排课算法、冲突检测、统计等），repository 负责数据访问，model 负责 GORM 数据模型，dto 负责请求/响应结构体
- middleware 负责跨切面逻辑，constants 统一维护错误码、常量
- 各层通过 Go 构造函数注入依赖，禁止把职责堆在 main.go，必须触达多个文件

后端本地构建与运行命令：
- 依赖安装：`go mod tidy`
- 编译检查：`go build ./...`
- 本地运行：`go run ./cmd/server`

部署方式：
- 开发模式：`go run ./cmd/server`
- 生产模式：`go build -o server ./cmd/server && ./server`（Docker 中由多阶段构建产物启动）

提供以下文件：
- `backend/Dockerfile`：Go 多阶段构建，运行 `/app/server`
- 根目录 `docker-compose.yml`：仅编排后端服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=gbschedule`
- `.env.example`

`backend/Dockerfile` 必须采用 Go 多阶段构建：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: gbschedule`
- 服务使用 `container_name`，带 `${COMPOSE_PROJECT_NAME:-gbschedule}` 前缀
- 后端服务内部端口统一为 `8080`
- 暴露端口：`${BACKEND_PORT:-19515}:8080`
- 使用命名卷持久化 SQLite 数据文件，挂载到容器内数据目录
- 后端服务配置 `healthcheck`（后端提供 `/health` 健康检查接口）
- 所有可变配置通过环境变量注入
- 支持 `docker compose up -d` 启动

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=gbschedule`
- `BACKEND_PORT`
- `DB_PATH`（SQLite 数据库文件路径，默认 `./data/gbschedule.db`）

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- API 文档地址（`/docs`，Swagger UI）
- 快速启动方式（Docker Compose 或本地运行）
- 主要 API 端点说明
- 技术栈（表格展示，后端技术栈写为 Go 1.22 + Gin + GORM + SQLite）
- 项目目录结构（代码块）
- 本地开发命令为 `go mod tidy` 和 `go run ./cmd/server`
- Docker 部署说明
- License

## Git 要求

代码编写完成后初始化 Git 仓库：
```bash
git init
git add -A
git commit -m "init: 项目初始化"
```

创建 `.gitignore` 文件，内容包含：
```
node_modules/
dist/
.env
.DS_Store
*.log
.idea/
.vscode/
coverage/
backend/bin/
backend/server
*.exe
*.test
coverage.out
*.db
*.sqlite3
```
