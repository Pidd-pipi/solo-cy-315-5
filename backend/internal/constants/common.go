package constants

// Pagination defaults and limits.
const (
	DefaultPage     = 1
	DefaultPageSize = 20
	MaxPageSize     = 200
)

// Adjustment action names.
const (
	ActionSwap = "swap"
	ActionMove = "move"
)
